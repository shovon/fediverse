package simpleld

// func Marshal(value any, context []Context) ([]byte, error) {
// 	if reflect.ValueOf(value).Kind() != reflect.Struct {
// 		return nil, errors.New("value must be a struct")
// 	}
// }
